用户组权限
root2组（超级管理员）
文件上传、下载、删除
用户管理（包括将用户分配到root2组）
最高权限

root组（管理员）
文件上传、下载、删除
不能管理用户权限

competition组（比赛用户)
文件上传、下载
不能删除文件

other组（普通用户）
仅文件下载


用户名: root2，密码: Huang@-1122（超级管理员）。用户名:kfzxroot，密码:kfzx@-1122(管理员)。用户名: a ，密码:Key-1122(普通用户)